import 'package:flutter/material.dart';

class CarDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Otobox'),
      ),
      body: Container(
        child: Center(
          child: Text('details pages'),
        ),
      ),
    );
  }
}
